apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "randomChart.fullname" . }}-test-connection"
  labels:
    {{- include "randomChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "randomChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
